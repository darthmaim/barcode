{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "postinstall": "npm run bootstrap",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^5.6.2",
    "typescript": "^4.8.4"
  }
}
